# Copy to .env and fill in. Do not commit .env (it is in .gitignore).

# Hugging Face token (optional — KaniTTS-2 models are public, no token required).
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Co-DM RAG pipeline (Sprint 2)
# OpenAI API key — used for text-embedding-3-small embeddings.
# Create at https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Pinecone API key — vector database for campaign knowledge.
# Create a free account at https://app.pinecone.io and generate an API key.
# PINECONE_API_KEY=pcsk_...

# Pinecone index name (must be created manually with 1536 dims, cosine metric).
# PINECONE_INDEX_NAME=co-dm-index

# Co-DM speech-to-text (Sprint 1)
# Deepgram API key — used by /ws/audio when mic chunks are finalized with audio_end.
# DEEPGRAM_API_KEY=...

# Deepgram model/language (optional defaults shown).
# DEEPGRAM_MODEL=nova-3
# DEEPGRAM_LANGUAGE=en-US

# Automatically send finalized voice transcripts to Co-DM query (1=true, 0=false).
# AUTO_QUERY_ON_VOICE=1

# Voice clone sample limits in seconds (defaults: min 3, max 60).
# CLONE_MIN_DURATION_SEC=3
# CLONE_MAX_DURATION_SEC=60

# Co-DM LLM brain (Sprint 3)
# Anthropic API key — used for Claude (claude-haiku-4-5) intent routing and responses.
# Create at https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-...

# Claude model for NPC dialogue and adventure parsing (default: claude-haiku-4-5-20251001).
# AI_MODEL=claude-haiku-4-5-20251001

# Max characters of adventure text sent to Claude for parsing (default: 160000).
# MAX_ADVENTURE_CHARS=160000
