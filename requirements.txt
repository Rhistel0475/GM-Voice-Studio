# Pocket TTS: full list. To avoid "resolution-too-deep", install in two steps:
#   pip install -r requirements-core.txt && pip install -r requirements-server.txt
# Kani TTS: LFM2 is in transformers 5.3+; kani-tts-2 expects torch>=2.8.0 (see requirements-tts.txt).
torch>=2.8.0
gradio>=6.6.0
kani-tts-2>=0.0.5
soundfile>=0.13.0
transformers>=5.3.0
fastapi>=0.100.0
uvicorn>=0.22.0
slowapi>=0.1.9
websockets>=12.0
anthropic>=0.50.0
# Co-DM RAG/LLM features: pip install -r requirements-rag.txt
# Optional (S3, Celery, PostgreSQL): pip install -r requirements-optional.txt
