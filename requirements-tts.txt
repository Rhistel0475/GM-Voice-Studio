# Kani TTS stack: pinned for compatibility with LFM2 and RoPE/speaker-emb.
# Install after requirements-core.txt: pip install -r requirements-tts.txt
# kani-tts-2 uses transformers.models.lfm2 (LFM2 added in transformers 5.3.0).
# Package declares torch>=2.8.0 and does not declare transformers; we pin both.
torch>=2.8.0
transformers>=5.3.0
kani-tts-2>=0.0.5
soundfile>=0.13.0
