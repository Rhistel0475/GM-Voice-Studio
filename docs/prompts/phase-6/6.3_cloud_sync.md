# Prompt 6.3 — Cloud Sync

Enable optional cloud synchronization.

## Requirements
- sync campaigns to cloud backend
- support background sync
- handle conflict resolution

## Functions
- syncCampaignToCloud(campaignId)
- pullCampaignFromCloud(campaignId)
