# Prompt 7.1 — LiveBoard Campaign Context Integration

Connect the existing LiveBoard UI to the shared Campaign Context store.

## Panels That Must Use Shared Data
- Active Scene Panel
- NPC Presence Panel
- Codex Quick Reference
- Session Log
- Narration Control Area

## Functions
- setActiveScene(sceneId)
- buildAiContext()
- generateSceneNarration()
- addSessionLogEntry(entry)
- getSessionLog()
- pushCodexEntryToScene(entryId)

Do not redesign the approved LiveBoard layout.
