/**
 * Live Board feature entry. Re-exports the page for routing.
 * Campaign store: data flows via CampaignContext (bridge). For direct store access
 * use store/selectors: useActiveScene, useSceneNpcs, useSceneCodexEntries, useActionLogForActiveScene.
 */
export { default } from "../../pages/LiveBoardPage";
