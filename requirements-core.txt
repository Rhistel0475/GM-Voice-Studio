# Step 1: TTS stack. Then: pip install -r requirements-server.txt
# Kani TTS: needs torch>=2.8.0, transformers>=5.3.0 (LFM2). See requirements-tts.txt for pins.
torch>=2.8.0
soundfile>=0.13.0
kani-tts-2>=0.0.5
transformers>=5.3.0
