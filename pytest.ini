[pytest]
# Skip slow tests (TTS model load) by default. Run with: pytest -m slow
addopts = -m "not slow" -v
markers =
    slow: marks tests that load the TTS model (deselect with -m "not slow")
    integration: marks tests that hit external services or full stack (run with -m integration)
